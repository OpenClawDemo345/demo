# iOS App bootstrap

Open this KMP project in Android Studio, then use:
- Kotlin Multiplatform plugin -> "Create iOS app"
- Link it to `:shared`

The shared module already includes iOS targets and Darwin Ktor client.
